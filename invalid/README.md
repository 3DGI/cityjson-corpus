# Invalid Fixtures

This directory holds syntactically valid but semantically invalid CityJSON
fixtures used to exercise rejection and validation paths.

Each file corresponds to a catalog case in `catalog/corpus.json` and an entry
in [invariants/corpus](../invariants/corpus.md).
