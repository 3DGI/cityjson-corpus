[project]
name = "cityjson-corpus"
version = "1.0.0"
description = "Shared benchmark corpus, profiles, and documentation for CityJSON tooling"
requires-python = ">=3.11"
dependencies = [
  "properdocs",
  "mkdocs-gen-files",
  "mkdocs-materialx",
  "jsonschema",
]

[tool.uv]
package = false
